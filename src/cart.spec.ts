

describe('cart', () => {

xit('should add book in cart', () => {

  // const cart = new Cart();
  // const xp = new Book('the new book on x-programming');
  // const learnBigData  = new Book(' big data of 2020 ');

  // cart.addBook(xp);
  // cart.addBook(learnBigData);

  // const getBookList = cart.getBookList();
  // expect(getBookList).toEqual([xp, learnBigData]);

        // create a cart
        // add  a book
        // add more book
        // get a list of book
        // check contain both books in same order

  throw new Error('work in progress');
});

it.todo('should remove book in cart');

it.todo('should get total of cart');

});
